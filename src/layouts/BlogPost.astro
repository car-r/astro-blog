---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Container from "./Container.astro";

export interface Props {
	content: {
		title: string;
		description: string;
		pubDate?: string;
		updatedDate?: string;
		heroImage?: string;
	};
}

const {
	content: { title, description, pubDate, updatedDate, heroImage },
} = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={description} />
		<style>
			.title {
				font-size: 2em;
				margin: 0.25em 0 0;
			}
			hr {
  				border-top: 1px solid #DDD;
				margin: 1rem 0;
			}
		</style>
	</head>

	<body class="bg-black" >
		<Header />
		<Container>
		<main>
			<article class="prose max-w-none prose-lg md:prose-xl prose-a:text-blue-500 text-stone-300 prose-blockquote:text-stone-300 prose-headings:text-stone-100 prose-strong:text-stone-300 prose-code:text-black ">
				{heroImage && (
					<img
						width={870}
						height={360}
						src={heroImage}
						alt=""
					/>
				)}
				<h1 class="title text-stone-100 font-bold">{title}</h1>
				{pubDate && <time class="text-stone-400">{pubDate}</time>}
				{updatedDate && <div class="text-stone-500">Last updated on <time>{updatedDate}</time></div>}
				<hr/>
				<slot />
			</article>
		</main>
		<Footer />
		</Container>
	</body>
</html>
